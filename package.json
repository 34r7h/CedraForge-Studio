{
  "name": "cedraforge-studio",
  "version": "1.0.0",
  "description": "Interactive, gamified development environment for building Move smart contracts on Cedra",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "keywords": [
    "cedra",
    "move",
    "smart-contracts",
    "blockchain",
    "developer-tools"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
